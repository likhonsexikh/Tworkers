{
  "prompts": [
    {
      "name": "update_packages",
      "description": "A script to update all Termux packages.",
      "task": "update all installed packages using pkg and clean up the cache."
    },
    {
      "name": "install_server",
      "description": "A script to install a basic web server (e.g., nodejs http-server).",
      "task": "install the 'http-server' package globally using npm."
    }
  ]
}
